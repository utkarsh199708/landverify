# Coder memory index

- [title-engine infra quirks](title-engine-infra-quirks.md) — Windows/Git Bash + docker-compose gotchas: no `.env*` files, minio via quay.io, `--wait` fails on exited one-shots, MSYS path mangling, worktree write rules
- [title-engine Java build quirks](title-engine-java-build-quirks.md) — root convention on leaf modules only, version-catalog access in subprojects, minimal checkstyle (OneTopLevelClass), -Werror, victools transitives, deterministic schema export, sandbox shell limits
