[project]
name = "llm-gateway"
version = "0.0.0"
description = "The one door to models: every model call goes through here (CLAUDE.md #5)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/llm_gateway"]
